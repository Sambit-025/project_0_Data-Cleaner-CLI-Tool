# project_0_Data-Cleaner-CLI-Tool
Project 0: Data Cleaner CLI Tool 

Concepts covered:  Variables, loops, strings, functions List, dict, set, tuple File reading Error handling 

💡 What it does: 
Takes CSV/log file 
Cleans nulls
Removes duplicates 
Outputs summary stats 

📌 Add DSA: 
Use hashmap (dict) for deduplication → O(n) Avoid nested loops (no O(n²))
